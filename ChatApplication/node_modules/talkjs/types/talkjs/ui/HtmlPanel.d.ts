import { Promise } from "../minipolyfill";
export default class HtmlPanel {
    show(): void;
    hide(): void;
    setHeight(height: number): void;
    isVisible(): boolean;
    destroy(): Promise<void>;
}
