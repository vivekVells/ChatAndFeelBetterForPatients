import { Promise } from "../minipolyfill";
import User from "./User";
export declare class Api {
    private readonly boken;
    readonly me: User;
    private readonly appId;
    private readonly serverHostname;
    readonly appLocale: string;
    private readonly sessionId;
    private readonly version;
}
export declare function register(appId: string, me: User, signature: string | undefined, serverHostname: string, sessionId: string): Promise<Api>;
