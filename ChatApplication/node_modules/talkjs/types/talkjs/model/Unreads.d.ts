import { UnreadMessage } from "./PublishedDomain";
export declare class Unreads {
    private _emitMessage;
    private _onNotificationClick;
    on(eventType: "change", handler: (messages: UnreadMessage[]) => void): void;
    off(eventType: "change", handler: (messages: UnreadMessage[]) => void): void;
}
